# devderrr-

A lightweight static snapshot of the `tip.gap.bo` donation page.

## Files

- `index.html` — single-page Thai/English donation page mockup with navigation, offline marquee, creator profile, notes, and support form.

## Preview

Open `index.html` directly in a browser, or serve the repository root with a static file server.
